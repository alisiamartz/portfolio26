---
const { text, tone = "default" } = Astro.props;
const cls =
  tone === "muted"
    ? "border-white/10 text-white/60"
    : "border-white/20 text-white/80";
---

<span class={`text-xs px-3 py-1 rounded-full border ${cls}`}>
  {text}
</span>